{
  "name": "zero-sync-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx @rocicorp/zero@1.3.0 start --db-url ${DATABASE_URL} --port ${PORT:-4848} --allowed-origins ${ZERO_ALLOWED_ORIGINS:-*}"
  }
}
